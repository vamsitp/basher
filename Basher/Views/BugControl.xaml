<UserControl
    x:Class="Basher.Views.BugControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Basher"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" Loaded="UserControl_Loaded" DoubleTapped="BugControl_DoubleTapped" Tapped="BugControl_Tapped">

    <Canvas>
        <StackPanel Orientation="Vertical" Name="MainControl" Loaded="MainControl_Loaded">
            <TextBlock Name="Age" Foreground="WhiteSmoke" TextWrapping="NoWrap" HorizontalAlignment="Center" HorizontalTextAlignment="Right" FontWeight="Bold" Loaded="Age_Loaded" />
            <Image Name="Bug" HorizontalAlignment="Center" CacheMode="BitmapCache" Loaded="Bug_Loaded" />
            <TextBlock Name="AssignedTo" Foreground="White" TextWrapping="WrapWholeWords" HorizontalAlignment="Center" HorizontalTextAlignment="Center" FontWeight="Bold" Loaded="AssignedTo_Loaded"/>
        </StackPanel>
    </Canvas>
</UserControl>
